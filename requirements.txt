# Core Telegram bot framework (v3)
aiogram>=3.3,<4

# HTTP client for backend (Blitz) and payments (Heleket) integrations
httpx>=0.27,<1

# YAML for locale files (en/fa)
PyYAML>=6,<7

# Optional: faster event loop on Linux
uvloop>=0.19; platform_system=="Linux"

# Async SQLite database
aiosqlite>=0.19,<1
